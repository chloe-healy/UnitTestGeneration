def square(x: int, y: int, z: int, a:int) -> str:
    if x == y == z == a
        return "Square"
    elif x == y and z == a:
        return "Rectangle"
    else:
        return "Not a Square"
